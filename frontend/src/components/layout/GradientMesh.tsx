/**
 * GradientMesh used to render decorative violet / aurora / gold backgrounds
 * for the premium-tier themes. Those themes were removed (`premium`,
 * `aurora`, `luxury`), so the component is now a no-op kept solely for the
 * existing import in `AppLayout`. Delete the import + this file the next
 * time you sweep the layout components.
 */
export default function GradientMesh(): null {
  return null
}
